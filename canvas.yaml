preset: package

namespace: Voltra\FilamentNavigation
user-auth-provider: App\User

paths:
  src: src
  resource: resources

factory:
  path: database/factories

migration:
  path: database/migrations
  prefix: ''

console:
  namespace: Voltra\FilamentNavigation\Console

model:
  namespace: Voltra\FilamentNavigation

provider:
  namespace: Voltra\FilamentNavigation\Providers

testing:
  namespace: Voltra\FilamentNavigation\Tests
